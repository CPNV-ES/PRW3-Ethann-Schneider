# Install
```bash
npm install
```

# Run

get your API key from https://openweathermap.org/ and set it as an environment variable before running the development server:

```bash
export OPENWEATHERMAP_API_KEY=<your_api_key_here> && npm run dev
```